import type { TokenUsageTranslations } from '../types';

export const tokenUsage: TokenUsageTranslations = {
  monthlyUsage: 'Monthly Usage',
  title: 'Token Usage',
  usageText: '{{consumed}} of {{limit}} tokens used ({{percentage}}%)'
};
